import React from 'react'

const Skills = (props) => (
	<div className="item">
		<p className="level-title">{props.item}</p>                                                                    
	</div>
);

export default Skills;